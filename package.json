{
  "name": "portfolio_sass_tm",
  "version": "1.0.0",
  "description": "Portfolio website using sass",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "node-sass -w scss/ -o dist/css/ --recursive"
  },
  "author": "haikalrozhan",
  "license": "ISC",
  "dependencies": {
    "node-sass": "^7.0.1"
  }
}
